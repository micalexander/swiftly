#!/usr/bin/env ruby

APP_ROOT = File.dirname(__FILE__)

$:.unshift( APP_ROOT )

require 'obi/Controller'
require 'yaml'

CONFIG = YAML.load_file("../.obiconfig") unless defined? CONFIG

delegate = Obi::Controller.new

delegate.config

Obi::Controller.start

